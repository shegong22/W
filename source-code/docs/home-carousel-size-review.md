# 首页轮播尺寸一致性验收记录

## 统一展示规则

四张轮播图均为 1920×1080。首页轮播容器固定为桌面端 `720px` 高、`16:9` 宽高比例；中等桌面宽度使用 `min(720px, 56.25vw)` 并设置 `620px` 最低高度；移动端固定为 `720px` 高。所有图片统一使用 `background-size: cover`、`background-repeat: no-repeat` 和相同的中心焦点规则，图片切换不会改变容器高度或文字/按钮坐标。

## 四张图逐槽位检查

| Slide | 资源 | 桌面裁切 | 移动裁切 | 主体位置 | 结果 |
|---|---|---|---|---|---|
| 01 | `tide-lab-hero-cleanroom_ffb4f311.jpg` | `cover / 50% 50%` | `cover / 58% 50%` | 右侧洁净生产设备与亚洲工作人员 | 通过 |
| 02 | `tide-lab-hero-synthesis_18f9f3f8.jpg` | `cover / 50% 50%` | `cover / 58% 50%` | 右侧合成/纯化设备与亚洲工作人员 | 通过 |
| 03 | `tide-lab-hero-batch_0a8104c6.jpg` | `cover / 50% 50%` | `cover / 58% 50%` | 右侧批次托盘和洁净操作人员 | 通过 |
| 04 | `tide-lab-hero-analytical_1e7ff08d.jpg` | `cover / 50% 50%` | `cover / 58% 50%` | 右侧分析仪器和亚洲实验室人员 | 通过 |

## 切换验收

自动切换仍使用原有 6.5 秒计时器，左右按钮只改变 `activeSlide`，不改变轮播容器尺寸、文字区域、进度条位置或按钮组位置。四个 slide 共用同一 DOM 框架和 CSS 尺寸规则；桌面首屏与手机首屏截图均确认框架边界、文字区和底部控制区保持一致。四张图片的源文件尺寸与主题不同，但视觉展示框架保持统一。
